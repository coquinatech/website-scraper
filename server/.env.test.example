# Example environment file for running LLM integration tests
# Copy this to .env.test and add your API keys
#
# To use:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Add your API keys to .env.test
# 3. Run tests: source .env.test && npm test

# OpenAI Configuration (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...

# Anthropic Configuration (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=sk-ant-...

# Test Configuration
DEFAULT_LLM_PROVIDER=mock
NODE_ENV=test
LOG_LEVEL=error  # Reduce noise during tests

# Cost Control - Use cheapest models for testing
OPENAI_DEFAULT_MODEL=gpt-4o-mini
ANTHROPIC_DEFAULT_MODEL=claude-3-haiku-20240307

# Optional: Test specific features
TEST_STREAMING=true
TEST_TOOL_CALLING=true
TEST_ERROR_HANDLING=true